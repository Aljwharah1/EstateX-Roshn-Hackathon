"""
`app` package initializer.

This file makes the `app` folder a proper Python package so relative
imports like `from .llm import ...` resolve when running via uvicorn.
"""

# package marker (no runtime code needed)
# Package initializer for app
# This file makes the `app` directory a Python package so relative imports work.
